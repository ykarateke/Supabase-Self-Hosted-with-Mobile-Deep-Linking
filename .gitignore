# ========================================================================
# Supabase Self-Hosted .gitignore
# ========================================================================
# This file prevents sensitive configuration and data from being committed

# ========================================================================
# 🔐 CRITICAL: Environment & Secrets (NEVER COMMIT THESE!)
# ========================================================================

# Environment files with real credentials
.env
.env.local
.env.production
.env.staging

# Backup of environment files
.env.backup
.env.old
.env.bak
.claude

# ========================================================================
# 🗄️ Database & Storage (Local Data)
# ========================================================================

# Database volumes and data
volumes/db/
volumes/storage/
volumes/logs/

# Database backups (may contain sensitive data)
*.sql
*.dump
backup_*
db_backup_*

# PostgreSQL data
postgres-data/
supabase-db-data/

# ========================================================================
# 🐳 Docker & Deployment
# ========================================================================

# Docker volumes
volumes/
docker-volumes/

# Docker Compose overrides (may contain secrets)
docker-compose.override.yml
docker-compose.local.yml
docker-compose.production.yml

# ========================================================================
# 📧 Email Templates (if containing sensitive info)
# ========================================================================

# Backup email templates (may contain real company info)
mail-template/*.backup
mail-template/*.bak
mail-template/*.old

# ========================================================================
# 🔑 SSL/TLS Certificates
# ========================================================================

# SSL certificates and private keys
*.pem
*.key
*.crt
*.cer
*.p12
*.pfx

# Certificate directories
certs/
ssl/
tls/

# ========================================================================
# 📝 Logs & Monitoring
# ========================================================================

# Application logs
logs/
*.log

# Monitoring data
monitoring/
metrics/

# ========================================================================
# 🛠️ Development & IDE
# ========================================================================

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Temporary files
*.tmp
*.temp
temp/
tmp/

# ========================================================================
# 🚀 Deployment & CI/CD
# ========================================================================

# Deployment scripts with secrets
deploy.sh
deploy-prod.sh
*.deploy

# CI/CD files (may contain secrets)
.github/workflows/deploy.yml
.gitlab-ci.yml
.circleci/config.yml

# Terraform state (if used)
*.tfstate
*.tfstate.backup
.terraform/

# Kubernetes secrets (if used)
k8s-secrets.yml
secrets.yml

# ========================================================================
# 📊 Analytics & Reports
# ========================================================================

# Analytics data
analytics/
reports/
stats/

# ========================================================================
# 🔧 Configuration Overrides
# ========================================================================

# Local configuration overrides
config.local.js
config.local.json
local.config.*

# Personal notes and documentation
NOTES.md
TODO.md
PERSONAL.md

# ========================================================================
# 📱 Mobile App Certificates (if stored here)
# ========================================================================

# Android keystore
*.keystore
*.jks

# iOS certificates
*.mobileprovision
*.p8

# ========================================================================
# 🌐 Domain & DNS
# ========================================================================

# DNS configuration files (may contain sensitive info)
dns-config.yml
domain-config.json

# ========================================================================
# 💾 Backup Files
# ========================================================================

# General backup files
*.backup
*.bak
backup/
backups/

# Archive files
*.tar.gz
*.zip
*.rar

# ========================================================================
# 🏗️ Build Artifacts
# ========================================================================

# Build directories
build/
dist/
out/

# Dependencies
node_modules/
vendor/

# ========================================================================
# ✅ SAFE TO COMMIT (Examples for reference)
# ========================================================================

# These files SHOULD be committed:
# README.md
# SETUP_GUIDE.md
# .env.example
# docker-compose-coolify.yml
# docker-compose.yml
# mail-template/*.html (template files without real data)
# .gitignore (this file)

# ========================================================================
# 📋 IMPORTANT NOTES
# ========================================================================

# 1. Always review files before committing
# 2. Use 'git status' to check what's being tracked
# 3. Use 'git add .' carefully - prefer explicit file names
# 4. If you accidentally commit secrets:
#    - Change all passwords/keys immediately
#    - Use git-filter-branch or BFG to clean history
#    - Force push to update remote repository
#
# 5. For team collaboration:
#    - Share .env.example instead of .env
#    - Document any additional files that need local configuration
#    - Use secure channels for sharing actual credentials
#
# ========================================================================